import {
  StyleSheet,
} from 'react-native';

export const topTabBarHeight = 51;

export const styles = StyleSheet.create({
  container: {
    height: topTabBarHeight,
    flexDirection: 'row',
  },
});
